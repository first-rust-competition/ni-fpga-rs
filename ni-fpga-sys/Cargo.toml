[package]
name = "ni-fpga-sys"
version = "0.1.0"
authors = ["Connor Worley <connorbworley@gmail.com>"]
edition = "2018"
license = "MIT"
description = "Rust bindings to the NI FPGA C API."

links = "NiFpga"
build = "build.rs"

[dependencies]
